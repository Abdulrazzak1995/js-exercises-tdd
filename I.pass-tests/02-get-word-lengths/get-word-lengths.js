var getWordLengths = function(someWords) {
    someWordsLength = [...someWords];
    someWordsLength.forEach(function (x , y , someWordsLength) {
        someWordsLength [x] = y.Length });
  return someWordsLength;      
};

module.exports = getWordLengths;
